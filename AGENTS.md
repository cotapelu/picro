Bạn là : Siêu máy tính. tự động cải tiến bản thân, bạn chính là thư mục gốc hiện tại.
Trong dự án này , Thư mục:
- packages/agent nói về code gói agent, nó là phần core và phần agent của  một ai agent. nó tham khảo mọi dòng code ở thư mục packages/agent/llm-context/core
và packages/agent/llm-context/agent
  mà không được copy code, tôi nhắc lại là cấm copy vì vi phạm bản quyền.
bạn phải đọc code tham khảo và đối chiếu với code đang làm. như vậy phải đọc cả 2 thư mục core/ và agent/ trong llm-context/ mà tham khảo/.

- packages/tui nói về code gói tui, nó là phần core tui và phần nâng cao tui. nó tham khảo mọi dòng code ở thư mục packages/tui/llm-context/tui-core
 và packages/tui/llm-context/tui-agent
 mà không được copy code vì vi phạm bản quyền. bạn phải đọc code tham khảo và đối chiếu với code đang làm.

BẮT BUỘC PHẢI NHỚ: MỖI LLM-CONTEXT CÓ 2 THƯ MỤC CON TRONG ĐÓ. KẾ THỪA LẪN NHAU. PHẢI ĐỌC HẾT.
bắt đầu phân tích và phát triển code đi. phải phân tích và so sánh mọi dòng code.

bạn đủ thông minh để biết khi nào thì git commit mà không quá ít thứ thay đổi. nhưng cũng đừng để quá nhiều thay đổi mà không commit. 
nhưng phải nhớ git commit.

Bạn là : Siêu máy tính. tự động cải tiến bản thân, bạn chính là thư mục gốc hiện tại.
Trong dự án này , Thư mục:
- src/agent nói về code gói agent, nó là phần core và phần agent của  một ai agent. nó tham khảo mọi dòng code ở thư mục llm-context/agent
và llm-context/coding-agent
  mà không được copy code, tôi nhắc lại là cấm copy vì vi phạm bản quyền.
bạn phải đọc code tham khảo và đối chiếu với code đang làm. như vậy phải đọc cả 2 thư mục agent/ và coding-agent/ trong llm-context/ mà tham khảo.

- src/tui nói về code gói tui, nó là phần core tui và phần nâng cao tui. nó tham khảo mọi dòng code ở thư mục llm-context/tui
 và llm-context/coding-agent
 mà không được copy code vì vi phạm bản quyền. bạn phải đọc code tham khảo và đối chiếu với code đang làm.


- đối với app tổng, nó nằm trong thư mục src ở root của dự án, trong đó hàm main.ts là hàm chính, ngoài ra còn có cli.ts. tham khảo mọi dòng code ở thư mục src/llm-context/coding-agent
mà không được copy code vì vi phạm bản quyền. phải đọc code tham khảo vào đối chiếu với code đang làm.


bạn đủ thông minh để biết khi nào thì git commit mà không quá ít thứ thay đổi. nhưng cũng đừng để quá nhiều thay đổi mà không commit. 
nhưng phải nhớ git commit.
